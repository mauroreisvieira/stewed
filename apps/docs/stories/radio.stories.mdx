import { Radio } from '../../../packages/components/src/index.ts';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

<Meta
    title="Components/Radio"
    argTypes={{ onChange: { action: 'change' } }}
    component={Radio}
/>

# Radio

Radio buttons allow the user to select one option from a set.

### Installation

```bash
npm install --save @stewed/components
```

### Import

```js
import { Radio } from '@stewed/components/radio';
```

## Examples

export const Template = (args) => <Radio {...args} />;

### Default

<Canvas>
    <Story
        name="Base"
        args={{
            skin: 'default',
            size: 'md',
            disabled: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Radio} />

import { Carousel } from '../../../../packages/react/src';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

<Meta title="Components/Carousel" component={Carousel} />

# Carousel

_TODO_

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Carousel } from '@stewed/react';
```

## Examples

export const Template = (args) => (
    <Carousel {...args}>
        {Array.from({ length: 6 }).map((_, index) => (
            <div
                key={index}
                style={{
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                    fontSize: 60,
                    color: 'white',
                    backgroundColor: '#f00',
                    width: '100%',
                    height: '300px',
                }}
            >
                {index + 1}
            </div>
        ))}
    </Carousel>
);

### Primary

<Canvas>
    <Story
        name="Primary"
        args={{
            loop: true,
            slidesPerView: 1,
            showNavigation: true,
            showIndicator: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Carousel} />

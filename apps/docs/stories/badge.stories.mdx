import { Badge } from '../../../packages/react/src';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

<Meta title="Components/Badge" component={Badge} />

# Badge

A badge consists of a small circle notification element.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Badge } from '@stewed/react';
```

## Examples

export const Template = (args) => (
    <Badge {...args}>
        <div style={{ width: 50, height: 50, backgroundColor: '#ddd' }} />
    </Badge>
);

### Default

<Canvas>
    <Story
        name="Base"
        args={{
            skin: 'primary',
            count: '99+',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Badge} />

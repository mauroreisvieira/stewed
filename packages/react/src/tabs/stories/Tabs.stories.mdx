import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Theme, Tabs } from "../../";
import { FiPackage, FiBell, FiCalendar } from "react-icons/fi";

<Meta
  title="Components/Tabs"
  component={Tabs}
  subcomponents={{ "Tabs List": Tabs.List, "Tabs Item": Tabs.Item }}
/>

# Tabs

Navigation between multiple pages or content sections.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Tabs } from "@stewed/react";
```

### Base

<Canvas>
  <Story name="Base">
    <Theme>
      <Tabs value="1">
        <Tabs.List>
          <Tabs.Item leftSlot={<FiPackage />} value="1">
            Orders
          </Tabs.Item>
          <Tabs.Item leftSlot={<FiBell />} value="2">
            Notifications
          </Tabs.Item>
          <Tabs.Item leftSlot={<FiBell />} value="3" disabled>
            Team
          </Tabs.Item>
          <Tabs.Item leftSlot={<FiCalendar />} value="4">
            Calendar
          </Tabs.Item>
        </Tabs.List>
      </Tabs>
    </Theme>
  </Story>
</Canvas>

## Props

<ArgsTable of={Tabs} />

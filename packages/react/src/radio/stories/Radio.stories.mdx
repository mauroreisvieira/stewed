import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Theme, Radio } from "../../";

<Meta title="Components/Radio" argTypes={{ onChange: { action: "change" } }} component={Radio} />

# Radio

Radio is a form element used for selecting one option from a list.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Radio } from "@stewed/react";
```

## Examples

export const Template = (args) => (
  <Theme>
    <Radio {...args} />
  </Theme>
);

### Base

<Canvas>
  <Story
    name="Base"
    args={{
      disabled: false,
      checked: false,
      size: "md",
      skin: "primary",
      children: "Label",
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Radio} />

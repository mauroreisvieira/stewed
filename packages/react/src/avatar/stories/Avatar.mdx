import { Story, Meta, Stories, ArgTypes } from "@storybook/blocks";
import * as AvatarStories from "./Avatar.stories";

# Avatar

When the image source is unavailable or the source is unspecified, the avatar component will display a default image.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Avatar } from "@stewed/react";
```

<Meta of={AvatarStories} />

<Stories />

## Props

<ArgTypes of={AvatarStories} />

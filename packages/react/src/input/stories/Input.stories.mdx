import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Theme, Input } from '../';

<Meta
    title="Components/Input"
    argTypes={{ onChange: { action: 'change' } }}
    component={Input}
/>

# Input

Form field to enter and edit single-line text.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Input } from '@stewed/react';
```

## Examples

export const Template = (args) => <Theme><Input {...args} /></Theme>;

### Base

<Canvas>
    <Story
        name="Base"
        args={{
            placeholder: 'Type your value...',
            defaultValue: '',
            skin: 'default',
            disabled: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Input} />

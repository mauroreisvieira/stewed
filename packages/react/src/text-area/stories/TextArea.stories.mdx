import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Theme, TextArea } from "../../";

<Meta
  title="Components/Text Area"
  argTypes={{ onChange: { action: "change" } }}
  component={TextArea}
/>

# TextArea

Form field to enter and edit multi line text.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { TextArea } from "@stewed/react";
```

## Examples

export const Template = (args) => (
  <Theme>
    <TextArea {...args} />
  </Theme>
);

### Base

<Canvas>
  <Story
    name="Base"
    args={{
      placeholder: "Type your value...",
      skin: "default",
      disabled: false,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={TextArea} />

import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Theme, TextField } from "../../";

<Meta
  title="Components/Text Field"
  argTypes={{ onChange: { action: "change" } }}
  component={TextField}
/>

# Text Field

Text field to enter and edit single-line text.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { TextField } from "@stewed/react";
```

## Examples

export const Template = (args) => (
  <Theme>
    <TextField {...args} />
  </Theme>
);

### Base

<Canvas>
  <Story
    name="Base"
    args={{
      placeholder: "Type your value...",
      defaultValue: "",
      skin: "default",
      disabled: false,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={TextField} />

import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import {
  FiSearch,
  FiPenTool,
  FiFile,
  FiFilePlus,
  FiMap,
  FiTrash,
  FiLock,
  FiMapPin,
} from "react-icons/fi";
import { Theme, FormField } from "../../";

<Meta title="Components/Form Field" component={FormField} />

# Form Field


### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { FormField } from "@stewed/react";
```

## Examples

export const Template = (args) => (
  <Theme>
    <FormField {...args} />
  </Theme>
);

### Base

<Canvas>
  <Story
    name="Base"
    args={{
      skin: "neutral",
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={FormField} />

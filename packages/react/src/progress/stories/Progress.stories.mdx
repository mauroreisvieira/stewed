import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Theme, Progress } from '../../';

<Meta title="Components/Progress" component={Progress} />

# Progress

Bar displaying progress for a task that takes a long time or consists of several steps.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Progress } from '@stewed/react';
```

## Examples

export const Template = (args) => <Theme><Progress {...args} /></Theme>;

### Base

<Canvas>
    <Story
        name="Base"
        args={{
            skin: 'primary',
            size: 'md',
            value: 50,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Progress} />

import { Canvas, Meta, ArgTypes, Stories } from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";

# Button

Button component is used to trigger an action or event, such as submitting a form, canceling an action, or performing a delete operation.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Button } from "@stewed/react";
```

<Meta of={ButtonStories} />

<Stories />

## Props

<ArgTypes of={ButtonStories} />

import { Meta, ArgTypes, Stories } from "@storybook/blocks";
import * as GradientStories from "./Gradient.stories";

# Gradient

A React component that manages the color stops in background gradients for its children. 

It allows you to define and control the gradient transitions, providing flexible styling for backgrounds within your components.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Gradient } from "@stewed/react";
```

<Meta of={GradientStories} />

<Stories />

## Props

<ArgTypes of={GradientStories} />

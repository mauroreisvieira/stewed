import { Canvas, Meta, Stories, ArgTypes } from "@storybook/addon-docs/blocks";
import * as SnackbarStories from "./Snackbar.stories";

# Snackbar

The Snackbar component displays temporarily and floats above the UI to provide users list of notifications on the screen.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Snackbar } from "@stewed/react";
```

<Meta of={SnackbarStories} />

<Stories />

## Props

<ArgTypes of={SnackbarStories} />

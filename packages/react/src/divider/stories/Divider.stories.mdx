import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import {
  FiSearch,
  FiPenTool,
  FiFile,
  FiFilePlus,
  FiMap,
  FiTrash,
  FiLock,
  FiMapPin,
} from "react-icons/fi";
import { Theme, Divider } from "../../";

<Meta title="Components/Divider" component={Divider} />

# Divider


### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Divider } from "@stewed/react";
```

## Examples

export const Template = (args) => (
  <Theme>
    <Divider {...args} />
  </Theme>
);

### Base

<Canvas>
  <Story
    name="Primary"
    args={{
      skin: "primary",
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


<ArgsTable of={Divider} />

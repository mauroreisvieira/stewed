import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { TiWarning } from 'react-icons/ti';
import { Alert, Theme } from '../../';

<Meta title="Components/Alert" component={Alert} />

# Alert

An alert displays a short and important message in a way that attracts the user attention.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Alert } from '@stewed/react';
```

## Examples

export const Template = (args) => <Theme><Alert {...args} /></Theme>;

### Base

<Canvas>
    <Story
        name="Base"
        args={{
            skin: 'info',
            title: 'Are you absolutely sure?',
            children:
                'This action cannot be undone. This will permanently delete your account and remove your data from our servers.',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Left Slot

<Canvas>
    <Story
        name="Left Slot"
        args={{
            skin: 'warning',
            title: 'Cannot connect to the database',
            leftSlot: <TiWarning size={24} />,
            children: 'We are unable to save any progress at this time.',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Alert} />

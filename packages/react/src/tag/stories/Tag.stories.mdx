import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Tag, Theme } from '../../';

<Meta title="Components/Tag" component={Tag} />

# Tag

Tag components add contextual metadata to a design.

### Installation

```bash
npm install --save @stewed/react
```

### Import

```js
import { Tag } from '@stewed/react';
```

## Examples

export const Template = (args) => <Theme><Tag {...args} /></Theme>;

### Base

<Canvas>
    <Story
        name="Base"
        args={{
            skin: 'warning',
            size: 'md',
            children: 'Premium',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Props

<ArgsTable of={Tag} />

# Button

A button triggers an event or action.

```bash
$ npm install --save @stewed/button
```

## Style

```scss
@import '~@stewed/button/scss';
```

## Props

Button component extends from `React.ButtonHTMLAttributes<HTMLButtonElement> & React.AnchorHTMLAttributes<HTMLAnchorElement>` interface, that provide special properties and methods for manipulating the options.

| Name          | Type                                                | Default   | Description                                |
| ------------- | --------------------------------------------------- | --------- | ------------------------------------------ | --- |
| `appearance?` | `One of<"primary", "success", "danger", "warning">` | 'primary' | The base styling to apply to the component |
| `size?`       | `One of<"sm", "md", "lg">``                         | 'md'      |                                            |
| `rounded?`    | `boolean`                                           | false     |                                            |
| `disabled?`   | `boolean`                                           | false     | Set if the button is disabled              |
| `className?`  | `string`                                            | -         | Add additional class name to the component |
| `children?`   | `React.ReactNode`                                   | -         | The value displayed within the component   |     |

## References

-   [ButtonHTMLAttributes](https://developer.mozilla.org/en-US/docs/Web/API/ButtonHTMLAttributes)
-   [AnchorHTMLAttributes](https://developer.mozilla.org/en-US/docs/Web/API/AnchorHTMLAttributes)
